var Day = temperature(25); // Declared a variable "Day" 
//dEclared the function "temperature" 
 
function temperature(temp)
{
if(temp>23.5)
{
console.log("It is a hot day"); 
// According to the conditions specified above, it is a hot day
}
else
{
console.log("It is a cold day");
}
}